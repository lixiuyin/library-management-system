# 复制为 .env 并填写实际值，勿将 .env 提交版本库
# 路径：work/.env（无论从何目录运行，都会从此处加载）

# ---------- 数据库（必填） ----------
# 使用 软件工程/docker.sh 启动的 MySQL 时：DB_USERNAME=root，DB_PASSWORD 与 docker.sh 中 MYSQL_ROOT_PASSWORD 一致，DB_HOST 用 127.0.0.1:3306
DB_USERNAME=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=127.0.0.1:3306
DB_NAME=book_system

# ---------- JWT（必填，生产请用随机长字符串） ----------
JWT_SECRET_KEY=your-random-secret-key-at-least-32-chars
# 可选，默认 1 天
# JWT_EXPIRATION_DAYS=1

# ---------- Flask 运行（可选） ----------
# FLASK_ENV=development
# FLASK_DEBUG=1
# FLASK_HOST=127.0.0.1
# FLASK_PORT=8080

# ---------- CORS（可选） ----------
# 默认 * 允许所有来源；生产可限制为前端地址，多个用逗号分隔
# CORS_ORIGINS=http://localhost:8081,http://localhost:8083
# CORS_ORIGINS=https://your-frontend-domain.com
