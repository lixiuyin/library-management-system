# 管理端前端

[English](README.md) | **中文**

> Vue 2 管理端应用，默认运行在 `http://localhost:8083`。主要功能包括图书管理、读者管理、借阅管理、管理员管理、重要操作记录和借阅排行。

## 运行

```bash
npm install
npm run serve
```

## 构建

```bash
npm run build
```

## 配置

开发环境 API 地址在 `.env.development` 中配置，默认指向 `http://127.0.0.1:8088`。
